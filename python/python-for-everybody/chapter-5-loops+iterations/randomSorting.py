# take an empty list, go through random numbers, and add those to the list, until it equals the specified number. I want to see how many numbers it picked before getting the number 

import random

values = []
raffle = 0
if raffle != 64:
    raffle = random.randint(0,100)
    values.append(random)